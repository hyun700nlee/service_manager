# 보안 정책

## 지원 버전

최신 주 버전의 최신 패치만 보안 업데이트를 받습니다. 상용 배포 전 `VERSION`과 릴리스 노트에 지원 종료일을 명시합니다.

## 취약점 제보

취약점은 공개 이슈로 등록하지 말고 제품 공급자가 계약 문서에 명시한 비공개 보안 연락처로 제보하십시오. 재현 절차, 영향 버전, 예상 영향과 로그의 비밀값 제거 여부를 포함해야 합니다.

## 기본 보안 동작

- 자격증명은 Windows DPAPI로 보호되고 JSON 내보내기에서 제외됩니다.
- SSH 호스트 키 지문이 없거나 변경되면 접속이 거부됩니다.
- Telnet은 명시적으로 위험을 확인한 작업에서만 허용됩니다.
- GUI와 엔진은 외부 포트나 IPC를 열지 않는 단일 프로세스로 실행되며 데이터 경로별 단일 인스턴스 잠금을 사용합니다.
- 자격증명 파일은 현재 Windows 사용자에게 묶인 DPAPI 암호문만 저장합니다. 데이터 폴더 접근 권한은 `%LOCALAPPDATA%`의 사용자 권한을 따릅니다.
- 원격 분석·텔레메트리는 기본적으로 전송하지 않습니다.
